ID: demoreservation
_schema-version: '2.1'
description: Demo reservation consolidated app
version: 0.0.1

modules:
 - name: demoreservation-node
   type: nodejs
   path: demoreservation-node
   parameters:
      host: demoreservation-node
      disk-quota: 256M
      memory: 256M
   provides:
    - name: demoreservation-node_api
      properties:
         url: ${default-url}
   requires:
    - name: application-logs_demoreservation
    - name: uaa_demoreservation
    - name: DEMO_RESERVATION_APIM_CUPS

 


 


 - name: demoreservation
   type: html5
   path: demoreservation
   parameters:
      disk-quota: 256M
      memory: 256M
   build-parameters:
      builder: grunt
   requires:
    - name: uaa_demoreservation
    - name: dest_demoreservation

resources:
 - name: application-logs_demoreservation
   parameters:
      service: application-logs
      service-plan: lite
   type: org.cloudfoundry.managed-service

 - name: uaa_demoreservation
   parameters:
      path: ./xs-security.json
      service-plan: application
      service: xsuaa
   type: org.cloudfoundry.managed-service

 - name: DEMO_RESERVATION_APIM_CUPS
   parameters:
      service-name: DEMO_RESERVATION_APIM_CUPS
   type: org.cloudfoundry.existing-service

 - name: dest_demoreservation
   parameters:
      service-plan: lite
      service: destination
   type: org.cloudfoundry.managed-service

